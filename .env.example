NEXT_PUBLIC_SUPABASE_URL=https://xxxx.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=...
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# Server-only — never expose to the browser
SUPABASE_SERVICE_ROLE_KEY=

# openai | anthropic | gemini
AI_PROVIDER=openai

OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o-mini

ANTHROPIC_API_KEY=
ANTHROPIC_MODEL=claude-sonnet-4-20250514

GEMINI_API_KEY=
GEMINI_MODEL=gemini-2.0-flash

# Stripe billing (required for production boot / readiness)
STRIPE_SECRET_KEY=sk_test_...
STRIPE_PRICE_MONTHLY=price_xxxxxxxxx
STRIPE_PRICE_YEARLY=price_xxxxxxxxx
STRIPE_WEBHOOK_SECRET=whsec_xxxxxxxxx

# Optional: skip fail-fast env validation (local tooling only — never in production)
# SKIP_ENV_VALIDATION=1
