import {
  computeCoverageRates,
  countEvidenceQuality,
  parseLeadingInt,
} from "@/lib/intelligence/historicalIntelligence54";

/** Re-export HI 5.4 coverage helpers — no duplicate rate math. */
export { computeCoverageRates, countEvidenceQuality, parseLeadingInt };
